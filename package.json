{
  "name": "neatocal",
  "private": true,
  "scripts": {
    "test": "bun test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write . && prettier --write '**/*.{md,html,css}'",
    "format:check": "biome format . && prettier --check '**/*.{md,html,css}'"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "prettier": "^3.8.1"
  }
}
